
NEXT:
    general:
        + database optimisation, RLS
        + mobile responsiveness
        + color scheme
        - notifications

    admin:
        home:
            - make the recent avctivities component
        
        organisations:
            - ! make the filter and search functionality
        
        organisationDetails:
            - ensure there is 1 admin when changing roles

        Projects:
            - ! makee the filter and search functionality
            + show project progress and tasks

        CreateProject:
            + save custom tmeplates in DB nad show them when selecting temapltes

        projectDetails:
            + fix used budget not showing
            - ensure there is atleast 1 supervisor when chnaging roles
            - make settings tab


        Vendors: 
            - make everything
        
        Settings:
            - make everything
            - ! make the setup for material avarage cost
    employees:
        organisatiopns:
            - let thme leave if they weant

+ add laoding to buttons: DONE
+ add toasts everywhere: DONE
+ material costing calculator
when approving a task for completion, first check if any requests are left automatically
dont showe budget to supervisor or anyone else othervthan admin
aadding budget per sq ft

TODO:
    + A front page.
    + A looks make over it
    + A code review for security.

    add privacy policy and terms of service
    add cookies and data pages

    setup billing and payment gateway

    when makign an org, assign a bdget and be able to change it and then we can assign budget to projects from that org
    
 list (billing + beta — manual unlock now; gateway later):
    + Phase A — Subscription truth
        + A1 subscriberStatus helpers (active plan, status, period end, cap interpretation) DONE
        + A2 load subscriber_entitlements with session / pass into app DONE
        + A3 owner shell rule: active subscriber (+ optional owns org), not signup-time admin DONE
        + A4 StoreHydrator branches on that rule DONE
    Phase B — Enforcement
        + B1 block org create unless active subscriber; link /billing
        + B2 max_orgs = count orgs where owner = user vs max_orgs
        + B3 max_projects (rule: under owned orgs + personal projects)
        + B4 max_users / seats (distinct members in scope; reuse does not consume another seat)
    Phase C — Gates & employees
        + C1 soft gate: renewal banner + billing CTA when subscription inactive but user owns orgs or still has paid tier on row; create-project copy split (lapsed vs teammate); lapsed owners still hydrate full org data
        + C2 employee empty states (organisations / projects / dashboard teammate hint); sidebar role "Member"; no org create for members (unchanged)
    Phase D — Billing UX
        + D1 Billing in sidebar + bottom nav (owner shell); Settings → Billing card + /billing link
        + D2 /billing copy: manual code first (gateway note removed from UI)
        + D3 After unlock: refetch entitlements, apply to store, runSessionStoreLoad, router.refresh()
    Phase E — Polish
        + E1 settings Billing card: plan, status badge, provider, period/trial dates, max orgs/projects/members
        + E2 user menu: name + email (fix bio / wrong “No name” copy)
        + E3 /statistics in sidebar + mobile nav for owner shell; page placeholder + member redirect
        E4 add a tutorial for first time users
        E5 smoke checklist (before ship): 09 limits in DB, org/project/seat caps, errors/toasts, subscriber vs member
    Phase F — Ship
        F1 prod env + Supabase auth URLs/emails
        F2 legal copy for manual / beta billing
        F3 beta comms + final launch pass
    Later: payment provider checkout + webhooks; Capacitor/Play; job costing/Tally; payouts